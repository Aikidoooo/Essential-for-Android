# Essential 機能境界

## ダウンローダー

- 本人が保存権限を持つ公開コンテンツのみを対象とします。
- 認証、Cookie、アカウントログイン、DRM、地域制限、暗号化、アクセス制御の回避は実装しません。
- URL、種類、画質、形式からアプリが固定したyt-dlpオプションだけを生成します。任意コマンド入力はありません。
- サイト側の仕様変更やyt-dlpが未対応のURLでは失敗する場合があります。失敗を成功として表示しません。
- 4K、8K、60fpsは、配信元に該当する素材がある場合だけ取得できます。
- MOVやAACなどへの変換では再エンコードが発生し、時間と一時容量を使用します。
- 動画は映像と音声を一時ファイルへ分離取得し、FFmpegで結合できた場合だけ最終ファイルを公開します。中間ファイルは保存先へ残しません。
- 画像のFHD、WQHD、4K、8Kは長辺の上限です。元画像より大きく引き伸ばして見せかけの解像度を作りません。

## QRスキャナー

- CameraXのCamera2バックエンドを通して、Androidの公開Camera Frameworkと端末メーカーのCamera HALを利用します。
- メーカー純正カメラアプリ専用の非公開アルゴリズムやLeica固有UIは呼び出しません。
- 実際の最小・最大倍率は各端末が公開するCameraCharacteristicsに従います。
- QR解析は端末内のバンドル済みML Kitモデルで行います。
- CameraXの`STRATEGY_KEEP_ONLY_LATEST`で待ちフレームを捨て、最初のQR検出時に解析を即停止します。実際の読み取り時間はカメラ、照明、フォーカス、SoCに左右されます。

## 行程表ジェネレーター

- 入力内容は端末内だけで処理します。
- PDF、文章、画像はユーザーがStorage Access Frameworkで選択した場所へ保存します。
- PDFは行程数に応じて自動改ページします。画像出力はAndroidの最大Bitmap制約により、非常に長い行程で高さを制限する場合があります。

## ファイル参照

- 元ファイルは書き換えず、生成物を`Download/Essential`へ別ファイルとして保存します。
- 動画・音声処理は端末のABIで利用可能なFFmpeg実装を使います。入力コーデックがFFmpegビルドに含まれない場合は変換できません。
- 圧縮は指定容量を上限としたビットレート計算です。既に小さいファイルは再エンコードせず複製します。
- 背景透過はGoogle ML Kit Subject Segmentationのダウンロード可能モデルを使います。初回はモデル準備まで待つ場合があり、被写体や背景によって輪郭精度が変わります。
